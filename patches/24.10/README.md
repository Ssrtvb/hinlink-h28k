# ImmortalWrt 24.10 HINLINK H28K 补丁集

- 基线：`fa66185054014e529e412477777fe73ab69a7c05`
- 最终开发树：`c715d69174a56a091c9002460c8957eb93af0407`
- 补丁格式：纯 diff

`0001`-`0003` 是 ImmortalWrt 24.10 缺少、但 H28K 必需的 RK3528 基础回移；`0010`-`0050` 与用户 25.12 补丁的板级拆分、名称和顺序对应。

按 `series` 顺序使用 `git apply`。整套补丁已在固定基线的干净源码树中重新应用，最终 Git tree 与开发树一致。

正式 tag 的直接应用情况见 `TAG_COMPATIBILITY.md`：`v24.10.1`–`v24.10.6` 全部通过；`v24.10.0` 仅因官方缺少 `phy-leds` 基础脚本而无法应用最后的 LED 补丁。本次按用户要求未对 tag 执行编译。
