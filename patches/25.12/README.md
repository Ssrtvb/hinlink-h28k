# ImmortalWrt 25.12 HINLINK H28K 补丁集

- 验证基线：正式版 `v25.12.1`，commit `a3378d1a2c15beb2faf4b0bce9c00f07143efa29`
- 补丁来源：用户原项目五补丁结构
- 补丁格式：LF 换行的纯 unified diff

按 `series` 顺序使用 `git apply`。本目录只包含 25.12 已具备 RK3528 基础支持后所需的五个 H28K 板级补丁，不包含 24.10 的 RK3528 基础回移。

最终补丁已在所有现有 25.12 正式 tag（`v25.12.0`、`v25.12.1`）上完成顺序应用、逆序还原和工作树清洁检查。按要求未进行编译。

版本兼容处理仅缩短易受新增设备影响的上下文并使用稳定分组或函数尾锚点；设备身份、DTS、U-Boot 配置、LAN/WAN、镜像 profile 和 LED 行为保持不变。PCIe RTL8169 LED 按 25.12 官方同类设备写法不绑定接口名。
